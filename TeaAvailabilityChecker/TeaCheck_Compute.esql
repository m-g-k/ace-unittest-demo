

CREATE COMPUTE MODULE TeaCheck_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN

		SET OutputRoot.JSON.Data.Result[] = SELECT T.Name AS TeaType, T.ID as SKU 
											FROM Database.TEA AS T 
											WHERE T.name LIKE InputRoot.XMLNSC.TeaCheck.Search;
		RETURN TRUE;
		
	END;

END MODULE;
